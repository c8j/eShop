@model List<eShop.models.Product>;

<h1>Produkter</h1>

<table>
    <thead>
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Price</th>
            <th>Add to cart</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var product in @Model)
        {
            <tr>
                <td>@product.ID</td>
                <td>@product.Name</td>
                <td>@product.Price</td>
                <td><a asp-controller="Cart" asp-action="AddProduct" asp-route-product="@product"><i
                            class="fa-solid fa-circle-plus"></i></a></td>
            </tr>
        }
    </tbody>
</table>
